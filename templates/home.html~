{% extends "base.html" %}
{% load djangomarkdown %}
{% block content %}
<div class="posts">
    {% for post in post_list %}
        <section class="post">
            <header class="post-header">
                <h2 class="post-title" href="% url "detail" id=post.id %}">{{ post.title }}</h2>
                    <p class="post-meta">
Time:<a class="post-author">{{ post.created_time|date:"Y /m /d" }}</a> 
<a class="post-category post-category-js" href="#">{{ post.category }}</a>
                    </p>
            </header>
                <div class="post-description">
                    <p>
        {{ post.abstract|djangomarkdown:'codehilite' }}
                    </p>
                </div>
                <a class="pure-button" href="{% url "detail" id=post.id %}">Read More >>> </a>
        </section>
    {% endfor %}
</div>
{% endblock %}


           
