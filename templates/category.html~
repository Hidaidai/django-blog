<!--点击分类进行搜索-->
{% extends "base.html" %}
{% load custom_markdown %}
{% block content %}
<div id="layout" class="pure-g">
<!--博客侧边栏-->
    <div class="sidebar pure-u-1 pure-u-md-1-4">
    <!--博客标题-->
        <div class="header">
            <h1 class="brand-title">海呆呆の博客</h1>
            <h2 class="brand-tagline">A problem is a chance to do your best！</h2>
            <nav class="nav">
                <ul class="nav-list">
                    <li><a href="" title=""
                                   rel="category">{{ model.category.name }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="pure-button" href="https://github.com/hidaidai">Github</a>
                    </li>
                    <li class="nav-item">
                        <a class="pure-button" href="http://weibo.com/dinosaurliu">Coding</a>
                    </li>
                     <li class="nav-item">
                        <a class="pure-button" href="http://weibo.com/dinosaurliu">Weibo</a>
                    </li>
                </ul>
            </nav>
        </div>
        <!--博客分类-->
        <hr style=" height:2px;border:none;border-top:2px dotted #185598;" />
         <div>
             <h4>博文分类：</h4>
             <div class="row">
                   <ul>
            {% for category in category_list %}
                       <li class="cat-item">
                        <a href="#">    {{ category.name }}</a>
                       </li>
            {% endfor %}
                  </ul>
          </div>
        </div>
                <!--博客标签-->
         <hr style=" height:2px;border:none;border-top:2px dotted #185598;" />
         <div>
             <h4>标签云：</h4>
             <div class="row">
                   <ul>
            {% for tag in tag_list %}
                       <li class="cat-item">
                        <a href="#">    {{ tag.tag_name }}</a>
                       </li>
            {% endfor %}
                  </ul>
          </div>
        </div>
    </div>
<!--博客主体-->
    <div class="content pure-u-1 pure-u-md-3-4">
        <div>
        <!--导航栏-->
           <div class="footer">
                <div class="pure-menu pure-menu-horizontal pure-menu-open">
                    <ul>
                        <li><a href="http://localhost:9000/">首页</a></li>
                        <li><a href="http://localhost:9000/archives/">归档</a></li>
                        <li><a href="http://localhost:9000/aboutme/">关于</a></li>
                    </ul>
                </div>
            </div>
<!--复用主体-超重要--><!--点击分类进行搜索-->
<div class="posts">
    {% for post in post_list %}
        <section class="post">
            <header class="post-header">
                  <h2 class="post-title">
                             <a href="{% url 'detail' id=post.id %}">{{ post.title }}</a></h2>
                             <p class="post-meta">
                                                                 时间:<a class="post-author" href="#">{{ post.date_time |date:"Y M d"}}</a> 
                                                                 分类:<a class="post-category post-category-js" href="{% url 'search_category' category=post.category %}">{{ post.category|title }}</a>
                                                                 标签:
                          {% for tag in post.tag.all %}
                            <a class="post-category post-category-pure" href="#">{{ tag }}</a>
                          {% endfor %}
                    </p>
            </header>

                <div class="post-description">
                    <p>
                        {{ post.abstract|custom_markdown }}
                    </p>
                </div>
                <a class="pure-button" href="{% url "detail" id=post.id %}">Read More >>> </a>
        </section>
    {% endfor %}
</div>
<!--复用主体-超重要-->
        </div>
    </div>
</div>
{% endblock %}


           
